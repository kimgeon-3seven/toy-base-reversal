# 개발 인계 상태

마지막 갱신: 2026-08-26

## 현재 완료 상태

- 일반 모드 1~5라운드의 방어 준비 → 방어 전투 → 공격 준비 → 공격 전투 순환 구현
- 건설 부품, 출격 포인트, 타워 2레벨 강화와 설계 유지·복원 구현
- 코스트별 코어 누수 피해 구현: 2코스트 6, 3코스트 12, 4코스트 20
- 지휘관 직접 이동, 집중 공격, 사거리 제한형 교란 구현
- 초기 방어 설계와 1~5라운드 고정 난이도 밸런스 구현
- 반복 가능한 정상 모드 밸런스 시뮬레이션 추가
- 마지막 전체 QA: ESLint 통과, TypeScript 통과, 68개 테스트 통과, 프로덕션 빌드 통과
- 브라우저 1라운드 QA: 방어 코어 40/120으로 성공, 공격 25.1초로 성공, 콘솔 오류 없음
- 일반 모드 완료 연출 후 진입하는 무한 챌린지 모드 구현
- 챌린지 웨이브 상한, 적 능력치 증가, 코어 체력 증가와 출격 포인트 상한 구현
- 챌린지 최고 완료 라운드와 최근 돌파시간의 세션 기록 및 HUD 표시 구현
- 챌린지 구현 후 ESLint, TypeScript, 77개 테스트와 프로덕션 빌드 통과
- 첫 실행 3단계 튜토리얼 구현. 안내 중 준비 타이머 정지, `Enter` 진행과 `Esc` 건너뛰기 지원
- 방어·공격 선택 정보의 상성 안내, 집중 공격·교란의 사거리와 유효 대상 시각화 구현
- 방어·공격 결과 화면의 정보 계층과 다음 행동 안내 개선
- UX 구현 후 ESLint, TypeScript, 80개 테스트와 프로덕션 빌드 통과
- 로컬 개발 서버 HTTP 200 응답 확인. 브라우저 플러그인의 신뢰 경로 오류로 이번 화면 자동 QA는 보류
- 다음 방어 라운드에 공격 유닛과 지휘관이 남던 상태 정리 오류 수정
- 일반 모드 최고 총 돌파시간과 챌린지 최고 라운드·동률 최단시간의 브라우저 영구 저장 구현
- 저장소 포트와 `localStorage` 어댑터를 분리하고 손상 데이터·저장소 접근 실패 시 안전한 세션 기록으로 대체
- 시작 화면 개인 기록 표시, 신기록 안내, `L` 두 번 입력을 통한 기록 초기화 구현
- 기록 저장 구현 후 ESLint, TypeScript, 87개 테스트와 프로덕션 빌드 통과
- 사용자 QA에서 다음 방어 라운드의 공격 유닛·지휘관 정리 확인 완료
- 기록 규칙 집중 테스트 7개, 전체 87개 회귀 테스트, 페이지 새로고침과 콘솔 오류 QA 통과
- 닉네임 편집, 익명 플레이어 ID, 챌린지 상위 10명과 내 순위 오버레이 구현
- 개인 챌린지 신기록 자동 제출과 서버 미설정·연결 실패 시 로컬 기록 유지 구현
- Supabase 순위 테이블, 최고 기록 갱신·조회 함수, 입력 검증 Edge Function과 환경 설정 예시 추가
- 온라인 순위표 구현 후 97개 회귀 테스트, ESLint, TypeScript, 프로덕션 빌드 통과
- 별도 브라우저 QA에서 순위표 오프라인 대체 화면과 닉네임 창 열기·취소, 접근성 대화상자 인식, 콘솔 오류 없음 확인
- `main` 게시 시 린트·97개 테스트·빌드를 거쳐 `dist`만 배포하는 GitHub Pages Actions 워크플로 구현
- Pages 배포 설정 후 로컬 전체 검사와 `/toy-base-reversal/` 정적 자산 경로 검증 통과
- 공개 배포에서 비어 있는 순위표 환경 변수를 온라인 설정으로 오인하던 시작 차단 오류 수정
- 3페이지 선행 설명을 한 화면의 장난감 전쟁 훅과 단계별 코치마크로 교체
- 최초 1회 상세 안내 저장, 이후 짧은 시작 화면과 방어 준비 중 `H` 다시 보기 구현
- 첫 라운드 방어 성공 후 2.3초 역할 반전 연출과 추천 부대 공격 준비 연결
- 공격 전투 중 `WASD → Q 집중 공격 → E 교란` 순서의 점진적 지휘관 안내 구현
- 방어 결과의 생존 시설·수훈 타워, 공격 결과의 다음 해금 정보 추가
- 캔버스에 현재 게임 단계와 안내 상태를 제공하는 접근성 레이블 추가
- 첫 3분 UX 구현 후 ESLint, TypeScript, 107개 테스트와 프로덕션 빌드 통과
- 로컬 브라우저 QA에서 시작 → 방어 → 역할 반전 → 공격 결과 흐름을 91.5초 안에 확인하고 콘솔 오류 없음 확인
- Kenney Tower Defense (Top-Down) 기반 타워·유닛·장애물·코어 스프라이트 적용
- 도메인 전투 이벤트와 Phaser 표현 계층을 분리해 투사체·피격·파괴·코어 피격·지휘관 능력 이펙트 구현
- Kenney 혼합 효과음과 OpenGameArt `Loop Town` BGM 적용, `M` 전체 음소거 지원
- 사용한 모든 외부 에셋의 CC0 원본 링크와 배포 라이선스 파일 보존
- 아트·사운드 적용 후 ESLint, TypeScript, 109개 테스트와 프로덕션 빌드 통과
- 로컬 브라우저에서 방어 준비 → 방어 성공 → 역할 반전 → 공격 성공 → 2라운드 준비를 확인했으며 콘솔 오류 없음
- OpenAI 이미지 생성으로 제작한 나무 책상·종이·펠트 장난감 놀이판 배경 적용
- 유닛과 지휘관의 이동·공격 8방향 전환, 타워의 360도 조준과 교란 흔들림 적용
- 모든 전투 스프라이트에 그림자, 이동 바운스, 피격 점멸과 발사 반동 적용
- BGM·효과음 개별 볼륨 조절, 전체 음소거와 브라우저 영구 저장 UI 구현
- 장면 재시작 시 기존 BGM을 재사용하고 남은 중복 트랙을 정리해 음악 겹침 방지
- 준비·전투·역할 반전의 게임 시간과 트윈을 함께 정지하는 일시정지 메뉴 구현
- 나가기 확인 후 현재 도전만 포기하고 개인 기록·닉네임·소리 설정을 유지한 채 시작 화면으로 복귀
- 오디오 설정·중복 트랙 규칙을 포함한 전체 122개 테스트, ESLint, TypeScript와 프로덕션 빌드 통과
- 로컬 브라우저에서 개별 볼륨 저장, 준비·전투 정지와 재개, 나가기 후 시작 화면 복귀를 검증하고 콘솔 오류 없음 확인
- 방어 처치율과 남은 코어 체력을 다음 공격 라운드의 출격 포인트로 전환하는 성과 보상 정책 구현
- 기본분 `기존 라운드 기준값 - 3`, 처치 보너스 0~5, 코어 보너스 0~3과 방어 S/A/B/C 등급 표시 구현
- 라운드·돌파시간 순위 규칙은 성과를 중복 계산하지 않도록 그대로 유지
- 환급 없는 `Delete` 파괴 기능을 제거하고 배치·이동·판매·강화의 실제 실행 취소/다시 실행 구현
- `S` 저장 지점 지정과 `R` 저장 지점 초기화를 실행 취소와 명확히 구분
- 방어 준비 하단의 타워·장애물 카드, 강화·설계 이력·저장·시작 버튼과 우측 웨이브 예고 구현
- 공격 준비의 유닛 카드와 3개 진입로별 대기열·추가·제거를 한 편성 보드로 구현
- 지휘관은 항상 2번 중앙 진입로에서 출발하도록 고정하고 위치 변경 입력과 UI를 제거
- 공격 편성 보드 높이를 252px에서 180px로 줄여 전장 가림 영역을 축소
- 무방비 배치 패배, 단일 타워 도배 대비 혼합 배치 우위, 성과 보상 적용 5라운드 돌파를 자동 밸런스 시나리오로 검증
- 현재 전체 QA: ESLint·TypeScript·프로덕션 빌드 통과, 41개 테스트 파일의 134개 테스트 통과
- 로컬 개발 서버는 `http://127.0.0.1:5173/toy-base-reversal/`에서 실행 중. 자동 브라우저는 최초 연결 실패 탭의 보안 정책으로 새 화면 QA를 완료하지 못함
- 상단에 `설계 → 방어 → 역할 반전 → 공략` 4단계 진행 표시를 추가하고 현재 단계를 강조하도록 구현
- 우측 상시 정보 패널을 현재 목표·핵심 수치·선택·바로 할 행동만 보여주는 임무 카드로 교체
- 시작 안내를 한 문장 훅과 클릭 가능한 단일 시작 버튼 중심으로 축소
- 방어·공격·캠페인 결과를 장난감 작전 카드 형태의 독립 결과 오버레이로 분리하고 주요 행동을 클릭 가능하게 구현
- 방어 등급과 `기본 + 처치 + 코어` 출격 포인트 계산을 순차적으로 공개하는 결과 연출 구현
- 방어 누수·코어 체력·시설 생존과 공격 실패 원인에 따라 다음 전략을 설명하는 피드백 조언 객체 및 테스트 추가
- 유닛 격파·타워 파괴·코어 피격 시 짧은 전투 콜아웃을 표시하도록 피드백 강화
- 버튼 그림자와 종이 테이프 장식을 적용해 장난감 작전 보드 UI 언어를 통일
- GitHub Pages용 절대 경로 빌드와 itch.io용 상대 경로 빌드를 분리하고 `build:itch` 및 배포 안내 문서 추가
- itch.io 업로드 ZIP 생성 완료: `toy-base-reversal-itch.zip`, 최상단 `index.html`, 43개 항목, 약 5.98MB
- UI 개편 후 ESLint·TypeScript·프로덕션 빌드와 42개 테스트 파일의 138개 테스트 통과
- 로컬 브라우저에서 시작 → 방어 → 방어 결과 → 역할 반전 → 공격 편성 흐름과 클릭 CTA를 확인했으며 콘솔 오류 없음
- Kingdom Rush Vengeance의 정보 계층과 전투 가독성을 참고하되 기존 설계·방어·역할 반전·공략 시스템과 밸런스는 그대로 유지
- 공격 이벤트에 피해량과 상성 유효 여부를 추가해 상성 공격에 `약점!` 콜아웃, 강화된 피격 점멸·흔들림·충격 링을 표시
- 팝건·관통포의 직선 투사체와 박격포의 포물선 투사체를 구분하고 명중 궤적·폭발 크기·파괴 연출을 강화
- 코어 체력 바·백분율과 지휘관 이름표·강조 링을 전장 위에 추가해 핵심 목표와 직접 조작 대상을 즉시 식별
- 설계 단계 마우스 위치에 배치 유령·타워 사거리·배치 가능/불가 색상과 구체적인 실패 사유를 표시
- 실제 경로 차단 여부까지 구조물을 배치하지 않고 계산하는 배치 미리보기 도메인 API와 회귀 테스트 추가
- 공격 전투 우측에 클릭 가능한 Q 집중 공격·E 교란 카드, 대상 선택 상태와 재사용 게이지를 추가
- 집중 공격·교란 대상의 유효 사거리, 호버 조준선, 지휘관 영향 반경과 집중 명령 연결선을 강화
- `dist-itch` 배포 산출물을 ESLint 검사 대상에서 제외해 전체 검사 명령이 배포 파일 존재 여부와 무관하게 동작
- 현재 전체 QA: ESLint·TypeScript·프로덕션 빌드 통과, 42개 테스트 파일의 140개 테스트 통과
- 800×800 축소 화면 브라우저 QA에서 배치 가능/불가 미리보기, 코어·지휘관 표시, Q/E 스킬 카드·사거리·호버 조준선을 확인했으며 콘솔 경고와 오류 없음
- Parryway의 간결한 전투 HUD를 참고해 상단 진행 표시를 축소하고 우측 임무 카드를 핵심 수치 3개 중심으로 재구성
- 방어 설계·공격 편성 패널은 준비 단계에만, Q/E 지휘관 능력 카드는 공격 전투에만 표시하는 단계별 UI 정책 객체와 회귀 테스트 추가
- 전투 중 반복 조작 설명을 제거하고 상태 알림은 일정 시간 뒤 사라지도록 변경
- 일시정지 메뉴에 현재 단계별 조작법 화면을 추가하고 itch.io 페이지용 한 문장 훅·핵심 특징·전체 조작 문안 작성
- 16:9, 16:10, Full HD 화면 비율에서 방어 준비·전투·공격 준비·공격 전투 UI가 잘리지 않는지 브라우저 확인, 콘솔 경고와 오류 없음
- 첫 일반 라운드 전용 `RoundOnboardingPolicy`를 추가해 온보딩 규칙과 2라운드 이후의 표준 규칙을 분리
- 첫 방어 준비를 12초로 줄이고 적을 9기에서 6기로 축소했으며, 완성된 초기 방어선과 추천 배치 칸 2개를 제공
- 첫 라운드 설계 패널은 팝건·블록 벽·방어 시작만 보여주고 강화·설계 이력·저장·되돌리기는 2라운드부터 제공
- 브라우저 QA에서 첫 방어 코어 120/120, 처치 6, 누수 0과 1분 이내 역할 반전 진입을 확인했으며 콘솔 오류 없음
- 첫 라운드 공격 돌파 후 2라운드에서 준비시간 30초, 적 18기, 박격포와 전체 설계 기능이 정상 복원되는 것을 확인
- 첫 1분 온보딩 구현 후 ESLint·TypeScript·53개 테스트 파일의 172개 테스트와 GitHub Pages·itch.io 빌드 통과
- 방어 결과에 처치율·코어 보존율 막대와 각 성과 보너스를 추가해 방어 성과가 공격 자원으로 바뀌는 과정을 시각화
- 역할 반전 문구를 `내 방어선 → 내 공략 대상`으로 강화하고 동일 설계를 최대 체력으로 복원한다는 정보를 명시
- 공격 준비 임무 카드에 동일 설계의 타워·장애물 구성, 현재 해금된 상성 유닛과 방어 보상 산식을 표시
- 공격 전투의 적 코어 50%·25% 돌파와 집중 공격 대상 파괴를 제한적인 전략 콜아웃으로 추가
- 공격 성공 결과에서 방어 등급·처치율·코어 보존율과 공략 시간·출격 포인트를 한 화면에 비교
- 2단계 구현 후 ESLint·TypeScript·55개 테스트 파일의 178개 테스트와 GitHub Pages·itch.io 빌드 통과
- 로컬 브라우저에서 방어 결과 → 역할 반전 → 공격 준비 → 공격 결과의 정보 계층을 확인했으며 콘솔 경고·오류 없음
- UI 버튼을 클릭·확정·무음으로 분류하고 연타 제한과 장면 생명주기 정리를 적용해 절제된 조작 효과음 구현
- 지휘관의 논리 좌표와 전투 밸런스는 유지하면서 76px 캐릭터가 외곽 그리드에서 전장 밖으로 돌출되지 않도록 화면 경계 정책 적용
- 최초 논리 이동 경계 변경안의 전투 회귀 6건을 자동 테스트로 발견하고 표현 계층 수정으로 전환
- 현재 전체 QA: ESLint·TypeScript·GitHub Pages·itch.io 빌드 통과, 67개 테스트 파일의 210개 테스트 통과
- 로컬 브라우저에서 지휘관 시작점·상단·하단·우하단 경계와 1라운드 공격 결과까지 확인했으며 콘솔 경고·오류 없음
- 일반 모드 현재 라운드, 방어 설계·강화, 건설 부품, 완료 결과와 누적 공격시간의 브라우저 자동 저장 구현
- 방어 준비 편집과 공격 편성 변경을 즉시 저장하고 전투 중 종료 시 각각 방어 준비·공격 준비 체크포인트로 복귀
- 시작 화면에 저장 라운드·부품·누적시간과 `이어하기`를 표시하고, 두 번 확인하는 `새 게임` 동작 추가
- 일반 모드 공격 성공 직후 다음 라운드 보상까지 반영한 체크포인트를 선저장해 결과 화면 종료에도 진행 보존
- 손상·구버전 저장 데이터 안전 폐기, 구조물 ID 충돌 방지와 챌린지 저장 제외 규칙 구현
- 이어하기 구현 후 ESLint·TypeScript·프로덕션 빌드 통과, 71개 테스트 파일의 230개 테스트 통과
- 로컬 브라우저에서 새 게임 자동 저장 → 새로고침 → 이어하기 표시 → 1라운드 설계 복원과 콘솔 오류 없음 확인
- 사용자 직접 QA로 일반 모드 1~5라운드 플레이와 이어하기 동작 확인 완료
- 최신 itch.io 상대 경로 빌드와 61개 항목·약 26.23MB 업로드 ZIP 재생성
- itch.io 패키지의 최상단 `index.html`, 폴더 접두사와 상대 자산 누락을 자동 검증하는 재현 가능한 패키징 스크립트 추가
- 공격 유닛 3종의 96개 보행 프레임이 동일한 발 기준선을 사용하는지 확인하고, 실제 이동 속도에 따라 약 5.75~10.25fps로 재생되는 `WalkAnimationCadence` 정책 추가
- NPC·플레이어 유닛이 같은 보행 규칙을 사용하며 정지·공격 전환 시 보행 배속이 남지 않도록 수정
- 보행 개선 후 ESLint·TypeScript·GitHub Pages·itch.io 빌드와 72개 테스트 파일의 234개 테스트 통과, 로컬 공격 라운드와 콘솔 오류 없음 확인
- 태엽 군단·고무줄 사수 보행 프레임을 `접지 → 통과 → 반대 접지 → 통과` 순서의 `v2` 시트로 재구성해 루프 경계의 자세 튐 완화
- 사용자 확인이 끝난 방패병 보행과 태엽 군단·고무줄 사수의 공격 애니메이션은 그대로 유지하고 자산 경로 회귀 테스트 추가
- 총 64개 신규 보행 프레임의 크기·발 기준선·루프 연결을 확인하고, 전체 73개 테스트 파일의 236개 테스트와 두 프로덕션 빌드 통과
- 로컬 브라우저에서 플레이어 태엽 군단과 NPC 고무줄 사수의 이동을 확인했으며 콘솔 오류 없음
- 태엽 군단·고무줄 사수 보행 원본을 8방향 4자세로 다시 제작하고, 마젠타 크로마키·불규칙 셀 추론·공통 축 정렬이 가능한 재현 스크립트와 생성 원본 보존
- 두 유닛만 6프레임 핑퐁 보행으로 확장하고 승인된 방패병 4프레임 보행과 모든 공격 시트는 그대로 유지
- 실표시 크기에서 인접 프레임 차이, 48프레임 기준선과 반복 프레임을 자동 검증해 태엽 군단 최소 차이 15.85, 고무줄 사수 10.21 확인
- 로컬 3라운드 방어에서 NPC 태엽 군단·고무줄 사수의 자세 변화와 기존 방패병 표시를 연속 캡처로 확인
- 사용자 제공 8.4초 플레이 영상을 0.14초 간격으로 확대 비교해 좌표·애니메이션은 정상이나 실제 발 실루엣이 거의 바뀌지 않는 원인 확인
- 태엽 군단·고무줄 사수 하체를 과장한 `v4` 생성 원본과 48프레임 투명 시트를 추가하고 `v3`를 포트폴리오 비교 기준으로 보존
- 방패병 68px은 유지하고 태엽 군단 64px·고무줄 사수 70px 가독성 크기를 별도 정책으로 적용
- 연속 태엽 개체에 논리 좌표와 무관한 ±4px 표현 간격을 적용해 발 겹침 완화
- 실제 표시 크기 하단 25%에서 태엽 군단 64픽셀, 고무줄 사수 54픽셀의 최소 발 실루엣 변화를 자동 검증
- v4 보행 개선 후 ESLint·TypeScript·75개 테스트 파일의 241개 테스트와 GitHub Pages·itch.io 빌드 통과, 브라우저 콘솔 오류 없음 확인
- 사용자 스프라이트 검토로 `v4`의 4개 원본을 `0-1-2-3-2-1`로 복제해 통과 자세가 부족했던 근본 원인 확인
- 태엽 군단·고무줄 사수를 `접지 → 하강 → 통과 → 반대 접지 → 하강 → 반대 통과`의 고유 6자세 `v5`로 다시 제작
- 북·북동·동·남동·남 5방향 생성 원본에서 좌측 3방향을 결정적으로 반전해 8방향 48프레임을 구성하고 핑퐁 복제 제거
- 방패병 보행은 그대로 유지하면서 태엽 군단·고무줄 사수만 0.78배 보행 재생 배율을 적용해 중간 자세 가독성 강화
- `AuthoredSixFrameCycle` 검증으로 방향별 6프레임 전부가 고유한지 검사하고, 태엽 군단 최소 프레임 차이 6.66·고무줄 사수 4.38 확인
- v5 보행 개선 후 ESLint·TypeScript·75개 테스트 파일의 242개 테스트와 프로덕션 빌드 통과, 로컬 3라운드 방어 화면 및 콘솔 오류 0건 확인

## 다음 작업

itch.io Draft 프로젝트에 새 빌드와 `ITCH_IO_PAGE_CONTENT.md` 문안을 적용하고, 커버 이미지·방어 준비·역할 반전·공격 전투 스크린샷을 완성한다. 이후 첫 3분 플레이의 직관성과 전투 피드백 밀도를 직접 QA하고, 제출용 3분 플레이 영상과 Codex 활용 설명을 제작한다.

## 재개 방법

1. 이 문서와 `GAME_DESIGN_DOCUMENT.md`를 확인한다.
2. 온라인 순위표 구현과 `SUPABASE_LEADERBOARD_SETUP.md`를 확인한다.
3. 다음 단계 계획을 제시하고 사용자 승인을 받은 뒤 개발을 시작한다.

개발 서버 기본 주소: `http://127.0.0.1:5173/toy-base-reversal/`
