# 개발 인계 상태

마지막 갱신: 2026-08-21

## 현재 완료 상태

- 일반 모드 1~5라운드의 방어 준비 → 방어 전투 → 공격 준비 → 공격 전투 순환 구현
- 건설 부품, 출격 포인트, 타워 2레벨 강화와 설계 유지·복원 구현
- 코스트별 코어 누수 피해 구현: 2코스트 6, 3코스트 12, 4코스트 20
- 지휘관 직접 이동, 집중 공격, 사거리 제한형 교란 구현
- 초기 방어 설계와 1~5라운드 고정 난이도 밸런스 구현
- 반복 가능한 정상 모드 밸런스 시뮬레이션 추가
- 마지막 전체 QA: ESLint 통과, TypeScript 통과, 68개 테스트 통과, 프로덕션 빌드 통과
- 브라우저 1라운드 QA: 방어 코어 40/120으로 성공, 공격 25.1초로 성공, 콘솔 오류 없음
- 일반 모드 완료 연출 후 진입하는 무한 챌린지 모드 구현
- 챌린지 웨이브 상한, 적 능력치 증가, 코어 체력 증가와 출격 포인트 상한 구현
- 챌린지 최고 완료 라운드와 최근 돌파시간의 세션 기록 및 HUD 표시 구현
- 챌린지 구현 후 ESLint, TypeScript, 77개 테스트와 프로덕션 빌드 통과
- 첫 실행 3단계 튜토리얼 구현. 안내 중 준비 타이머 정지, `Enter` 진행과 `Esc` 건너뛰기 지원
- 방어·공격 선택 정보의 상성 안내, 집중 공격·교란의 사거리와 유효 대상 시각화 구현
- 방어·공격 결과 화면의 정보 계층과 다음 행동 안내 개선
- UX 구현 후 ESLint, TypeScript, 80개 테스트와 프로덕션 빌드 통과
- 로컬 개발 서버 HTTP 200 응답 확인. 브라우저 플러그인의 신뢰 경로 오류로 이번 화면 자동 QA는 보류
- 다음 방어 라운드에 공격 유닛과 지휘관이 남던 상태 정리 오류 수정
- 일반 모드 최고 총 돌파시간과 챌린지 최고 라운드·동률 최단시간의 브라우저 영구 저장 구현
- 저장소 포트와 `localStorage` 어댑터를 분리하고 손상 데이터·저장소 접근 실패 시 안전한 세션 기록으로 대체
- 시작 화면 개인 기록 표시, 신기록 안내, `L` 두 번 입력을 통한 기록 초기화 구현
- 기록 저장 구현 후 ESLint, TypeScript, 87개 테스트와 프로덕션 빌드 통과
- 사용자 QA에서 다음 방어 라운드의 공격 유닛·지휘관 정리 확인 완료
- 기록 규칙 집중 테스트 7개, 전체 87개 회귀 테스트, 페이지 새로고침과 콘솔 오류 QA 통과
- 닉네임 편집, 익명 플레이어 ID, 챌린지 상위 10명과 내 순위 오버레이 구현
- 개인 챌린지 신기록 자동 제출과 서버 미설정·연결 실패 시 로컬 기록 유지 구현
- Supabase 순위 테이블, 최고 기록 갱신·조회 함수, 입력 검증 Edge Function과 환경 설정 예시 추가
- 온라인 순위표 구현 후 97개 회귀 테스트, ESLint, TypeScript, 프로덕션 빌드 통과
- 별도 브라우저 QA에서 순위표 오프라인 대체 화면과 닉네임 창 열기·취소, 접근성 대화상자 인식, 콘솔 오류 없음 확인
- `main` 게시 시 린트·97개 테스트·빌드를 거쳐 `dist`만 배포하는 GitHub Pages Actions 워크플로 구현
- Pages 배포 설정 후 로컬 전체 검사와 `/toy-base-reversal/` 정적 자산 경로 검증 통과
- 공개 배포에서 비어 있는 순위표 환경 변수를 온라인 설정으로 오인하던 시작 차단 오류 수정
- 3페이지 선행 설명을 한 화면의 장난감 전쟁 훅과 단계별 코치마크로 교체
- 최초 1회 상세 안내 저장, 이후 짧은 시작 화면과 방어 준비 중 `H` 다시 보기 구현
- 첫 라운드 방어 성공 후 2.3초 역할 반전 연출과 추천 부대 공격 준비 연결
- 공격 전투 중 `WASD → Q 집중 공격 → E 교란` 순서의 점진적 지휘관 안내 구현
- 방어 결과의 생존 시설·수훈 타워, 공격 결과의 다음 해금 정보 추가
- 캔버스에 현재 게임 단계와 안내 상태를 제공하는 접근성 레이블 추가
- 첫 3분 UX 구현 후 ESLint, TypeScript, 107개 테스트와 프로덕션 빌드 통과
- 로컬 브라우저 QA에서 시작 → 방어 → 역할 반전 → 공격 결과 흐름을 91.5초 안에 확인하고 콘솔 오류 없음 확인

## 다음 작업

첫 3분 UX를 GitHub Pages에 재배포하고 공개 URL을 최종 QA한다. 이후 제출용 3분 플레이 영상과 Codex 활용 설명을 우선 제작하고, 최소 시각·사운드 폴리시 및 Supabase 온라인 순위표 연결은 남은 시간 범위 안에서 진행한다.

## 재개 방법

1. 이 문서와 `GAME_DESIGN_DOCUMENT.md`를 확인한다.
2. 온라인 순위표 구현과 `SUPABASE_LEADERBOARD_SETUP.md`를 확인한다.
3. 다음 단계 계획을 제시하고 사용자 승인을 받은 뒤 개발을 시작한다.

개발 서버 기본 주소: `http://127.0.0.1:5173/toy-base-reversal/`
